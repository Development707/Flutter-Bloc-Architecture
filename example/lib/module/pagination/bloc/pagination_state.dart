part of "pagination_bloc.dart";

/// State for [PaginationBloc]
@freezed
class PaginationState with _$PaginationState {
  /// State for [PaginationBloc]
  const factory PaginationState() = _PaginationState;
}
